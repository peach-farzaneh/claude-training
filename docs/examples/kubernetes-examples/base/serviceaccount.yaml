apiVersion: v1
kind: ServiceAccount
metadata:
  name: worker
  labels:
    app: worker
  annotations:
    # IRSA annotation — replace with your actual IAM role ARN
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/worker-role
